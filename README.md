# LC3Sim - an LC3 emulator with a bad name
## Building
To build the current version, navigate to the project root and run the following commands:
`` mkdir build
`` cd build/
`` cmake ..
`` cmake --build .
